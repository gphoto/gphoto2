Till Kamppeter <till.kamppeter@gmx.net>

One should make the options also possible on the command 
line, for example:

gphoto2 -o Zoom=15.3 -o Aperture=F2.8 -o LensMode=Macro --capture-image

Hans Ulrich Niedermann <gp@n-dimensional.de>

And re-design the complete command line syntax into something
consistent, powerful and easy to understand, using one command line
parser instead of two.
